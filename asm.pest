alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }
hex = { '0'..'9' | 'A'..'F' | 'a'..'f' }

hexnum = { (hex)+ ~ "h" }
binnum = { ('0'..'1')+ ~ "b" }
decnum = { ('0'..'9')+ }


 = { (hexnum | binnum | decnum) }
Ident = { (alpha | digit | "_")+ }
Arg = { (Ident | Num) }

Label = { Ident ~ ":" }
Instruction = { 
    Ident ~ ( (Ident | Num) ~ ","? )* ~ "\n"
}

Line = { (Label+ | Instruction) }
    
File = { SOI ~ (Line ~ ("\n" | "\r\n")?)* ~ EOI }